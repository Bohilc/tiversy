export enum RouterName {
  LOGIN = "Login Page",
  HOME = "Home Page",
  CLIENT_SEARCH = "Client Search",
}

export enum RouterPath {
  LOGIN = "/",
  HOME = "/home",
  CLIENT_SEARCH = "/homeClientSearch",
}
